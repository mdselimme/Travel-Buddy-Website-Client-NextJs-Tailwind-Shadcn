

// const SERVER_API_URL = process.env.NEXT_PUBLIC_SERVER_API_URL || "http://localhost:5000/api/v1";


// //SERVER FETCH HELPER FUNCTION
// export const serverFetch = async (endpoint: string, options: RequestInit = {}):Promise<Response> => {

//     const {headers, ...restOptions} = options;

//     //to stop loop
    

// };